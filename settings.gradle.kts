pluginManagement {
    repositories {
        gradlePluginPortal()
        maven("https://repo.sayandev.org/snapshots")
    }
}

plugins {
    id("org.sayandev.stickynote.settings") version "1.7.64"
}
rootProject.name = ("TeleportBow")
